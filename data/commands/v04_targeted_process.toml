
[meta]
category = "process"
difficulty = "basic"
description = "针对基础用法具体操作、方向和选项的三题练习；只在模拟实验环境中输入。"

[[commands]]
id = "v04-ex-qa-6966ed174f"
command = "ps -eo pid,user,%cpu,args --sort=-%cpu"
summary = "按 CPU 使用率从高到低排列全部进程"

[[commands.tokens]]
text = "ps"
desc = "按 CPU 使用率从高到低排列全部进程"

[[commands.tokens]]
text = " -eo"
desc = "本次操作参数：-eo。按 CPU 使用率从高到低排列全部进程"

[[commands.tokens]]
text = " pid,user,%cpu,args"
desc = "本次操作参数：pid,user,%cpu,args。按 CPU 使用率从高到低排列全部进程"

[[commands.tokens]]
text = " --sort=-%cpu"
desc = "本次操作参数：--sort=-%cpu。按 CPU 使用率从高到低排列全部进程"

[commands.dictation]
prompt = "按 CPU 使用率从高到低排列全部进程"
answers = ["ps -eo pid,user,%cpu,args --sort=-%cpu"]

[[commands]]
id = "v04-ex-qa-d0095cc442"
command = "ps -u www-data -o pid,%cpu,args --sort=-%cpu"
summary = "对 Web 服务账号的进程按 CPU 使用率排序"

[[commands.tokens]]
text = "ps"
desc = "对 Web 服务账号的进程按 CPU 使用率排序"

[[commands.tokens]]
text = " -u"
desc = "本次操作参数：-u。对 Web 服务账号的进程按 CPU 使用率排序"

[[commands.tokens]]
text = " www-data"
desc = "本次操作参数：www-data。对 Web 服务账号的进程按 CPU 使用率排序"

[[commands.tokens]]
text = " -o"
desc = "本次操作参数：-o。对 Web 服务账号的进程按 CPU 使用率排序"

[[commands.tokens]]
text = " pid,%cpu,args"
desc = "本次操作参数：pid,%cpu,args。对 Web 服务账号的进程按 CPU 使用率排序"

[[commands.tokens]]
text = " --sort=-%cpu"
desc = "本次操作参数：--sort=-%cpu。对 Web 服务账号的进程按 CPU 使用率排序"

[commands.dictation]
prompt = "对 Web 服务账号的进程按 CPU 使用率排序"
answers = ["ps -u www-data -o pid,%cpu,args --sort=-%cpu"]

[[commands]]
id = "v04-ex-qa-cf19598c07"
command = "ps -eo pid,%cpu,%mem,args --sort=-%cpu"
summary = "按 CPU 排序时同时比较内存使用比例"

[[commands.tokens]]
text = "ps"
desc = "按 CPU 排序时同时比较内存使用比例"

[[commands.tokens]]
text = " -eo"
desc = "本次操作参数：-eo。按 CPU 排序时同时比较内存使用比例"

[[commands.tokens]]
text = " pid,%cpu,%mem,args"
desc = "本次操作参数：pid,%cpu,%mem,args。按 CPU 排序时同时比较内存使用比例"

[[commands.tokens]]
text = " --sort=-%cpu"
desc = "本次操作参数：--sort=-%cpu。按 CPU 排序时同时比较内存使用比例"

[commands.dictation]
prompt = "按 CPU 排序时同时比较内存使用比例"
answers = ["ps -eo pid,%cpu,%mem,args --sort=-%cpu"]

[[commands]]
id = "v04-ex-qa-dadb35a8b3"
command = "jobs -p %1"
summary = "只输出当前 shell 一号作业的进程组首进程 PID"

[[commands.tokens]]
text = "jobs"
desc = "只输出当前 shell 一号作业的进程组首进程 PID"

[[commands.tokens]]
text = " -p"
desc = "本次操作参数：-p。只输出当前 shell 一号作业的进程组首进程 PID"

[[commands.tokens]]
text = " %1"
desc = "本次操作参数：%1。只输出当前 shell 一号作业的进程组首进程 PID"

[commands.dictation]
prompt = "只输出当前 shell 一号作业的进程组首进程 PID"
answers = ["jobs -p %1"]

[[commands]]
id = "v04-ex-qa-40ca04ac79"
command = "jobs -p %2"
summary = "只读取当前 shell 二号实验作业 PID"

[[commands.tokens]]
text = "jobs"
desc = "只读取当前 shell 二号实验作业 PID"

[[commands.tokens]]
text = " -p"
desc = "本次操作参数：-p。只读取当前 shell 二号实验作业 PID"

[[commands.tokens]]
text = " %2"
desc = "本次操作参数：%2。只读取当前 shell 二号实验作业 PID"

[commands.dictation]
prompt = "只读取当前 shell 二号实验作业 PID"
answers = ["jobs -p %2"]

[[commands]]
id = "v04-ex-qa-090b0d6170"
command = "jobs -rp"
summary = "仅输出当前 shell 正在运行作业的 PID"

[[commands.tokens]]
text = "jobs"
desc = "仅输出当前 shell 正在运行作业的 PID"

[[commands.tokens]]
text = " -rp"
desc = "本次操作参数：-rp。仅输出当前 shell 正在运行作业的 PID"

[commands.dictation]
prompt = "仅输出当前 shell 正在运行作业的 PID"
answers = ["jobs -rp"]
