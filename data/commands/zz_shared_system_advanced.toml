
[meta]
category = "system"
difficulty = "advanced"
description = "跨模块共用的规范命令（由内容归一化维护）"

[[commands]]
id = "shared-5f6840c0ef5020b1"
command = "echo -e 'cat\\ncut\\ncot\\ncart' | grep 'c.t'"
summary = "匹配 c 加任意一个字符加 t 的模式；cart 不匹配因为 c 和 t 之间有两个字符"
simulated_output = "cat\ncut\ncot"

[[commands.tokens]]
text = "echo -e 'cat\\ncut\\ncot\\ncart' | grep 'c.t'"
desc = "匹配 c 加任意一个字符加 t 的模式；cart 不匹配因为 c 和 t 之间有两个字符"

[commands.dictation]
prompt = "匹配 c 加任意一个字符加 t 的模式；cart 不匹配因为 c 和 t 之间有两个字符"
answers = ["echo -e 'cat\\ncut\\ncot\\ncart' | grep 'c.t'"]

[[commands]]
id = "shared-0fb096b6071a53c2"
command = "ls /etc | grep 'h..t'"
summary = "在 /etc 目录下查找文件名中包含 h 加两个任意字符加 t 的文件"
simulated_output = "host.conf\nhosts"

[[commands.tokens]]
text = "ls /etc | grep 'h..t'"
desc = "在 /etc 目录下查找文件名中包含 h 加两个任意字符加 t 的文件"

[commands.dictation]
prompt = "在 /etc 目录下查找文件名中包含 h 加两个任意字符加 t 的文件"
answers = ["ls /etc | grep 'h..t'"]

[[commands]]
id = "shared-34a4c271befb01f4"
command = "echo -e 'ggle\\ngogle\\ngoogle\\ngooogle' | grep 'go*gle'"
summary = "o 出现零次或多次都匹配，所以 ggle 到 gooogle 全部被选中"
simulated_output = "ggle\ngogle\ngoogle\ngooogle"

[[commands.tokens]]
text = "echo -e 'ggle\\ngogle\\ngoogle\\ngooogle' | grep 'go*gle'"
desc = "o 出现零次或多次都匹配，所以 ggle 到 gooogle 全部被选中"

[commands.dictation]
prompt = "o 出现零次或多次都匹配，所以 ggle 到 gooogle 全部被选中"
answers = ["echo -e 'ggle\\ngogle\\ngoogle\\ngooogle' | grep 'go*gle'"]

[[commands]]
id = "shared-6ce5f988fa252d6e"
command = "grep 'error.*timeout' /var/log/syslog"
summary = "搜索同一行中先出现 error 后出现 timeout 的日志条目，中间可以有任意内容"
simulated_output = "Mar  8 10:15:23 server kernel: error: connection timeout after 30s\nMar  8 14:22:01 server app[1234]: error reading socket: timeout exceeded"

[[commands.tokens]]
text = "grep 'error.*timeout' /var/log/syslog"
desc = "搜索同一行中先出现 error 后出现 timeout 的日志条目，中间可以有任意内容"

[commands.dictation]
prompt = "搜索同一行中先出现 error 后出现 timeout 的日志条目，中间可以有任意内容"
answers = ["grep 'error.*timeout' /var/log/syslog"]

[[commands]]
id = "shared-7faabda4b08594e4"
command = "echo -e 'gl\\ngol\\ngool\\ngoool' | grep -E 'go+l'"
summary = "匹配 g 后面跟一个或多个 o 再跟 l 的模式，gl 因为没有 o 而不匹配"
simulated_output = "gol\ngool\ngoool"

[[commands.tokens]]
text = "echo -e 'gl\\ngol\\ngool\\ngoool' | grep -E 'go+l'"
desc = "匹配 g 后面跟一个或多个 o 再跟 l 的模式，gl 因为没有 o 而不匹配"

[commands.dictation]
prompt = "匹配 g 后面跟一个或多个 o 再跟 l 的模式，gl 因为没有 o 而不匹配"
answers = ["echo -e 'gl\\ngol\\ngool\\ngoool' | grep -E 'go+l'"]

[[commands]]
id = "shared-e280a2ee37a0bba4"
command = "echo -e 'abc\\na1b2c3\\n123\\nabc123' | grep -E '[0-9]+'"
summary = "匹配包含一个或多个连续数字的行"
simulated_output = "a1b2c3\n123\nabc123"

[[commands.tokens]]
text = "echo -e 'abc\\na1b2c3\\n123\\nabc123' | grep -E '[0-9]+'"
desc = "匹配包含一个或多个连续数字的行"

[commands.dictation]
prompt = "匹配包含一个或多个连续数字的行"
answers = ["echo -e 'abc\\na1b2c3\\n123\\nabc123' | grep -E '[0-9]+'"]

[[commands]]
id = "shared-d750ecb50dcb07a2"
command = "echo -e 'http://example.com\\nhttps://example.com\\nhttpss://bad' | grep -E 'https?://'"
summary = "匹配 http:// 和 https:// 两种协议前缀，s 是可选的"
simulated_output = "http://example.com\nhttps://example.com"

[[commands.tokens]]
text = "echo -e 'http://example.com\\nhttps://example.com\\nhttpss://bad' | grep -E 'https?://'"
desc = "匹配 http:// 和 https:// 两种协议前缀，s 是可选的"

[commands.dictation]
prompt = "匹配 http:// 和 https:// 两种协议前缀，s 是可选的"
answers = ["echo -e 'http://example.com\\nhttps://example.com\\nhttpss://bad' | grep -E 'https?://'"]

[[commands]]
id = "shared-a30a2d57137c145e"
command = "echo -e '42\\n-7\\n+10\\n3.14' | grep -E '^-?[0-9]+$'"
summary = "匹配可选负号后跟一个或多个数字的整数，+10 和 3.14 不匹配"
simulated_output = "42\n-7"

[[commands.tokens]]
text = "echo -e '42\\n-7\\n+10\\n3.14' | grep -E '^-?[0-9]+$'"
desc = "匹配可选负号后跟一个或多个数字的整数，+10 和 3.14 不匹配"

[commands.dictation]
prompt = "匹配可选负号后跟一个或多个数字的整数，+10 和 3.14 不匹配"
answers = ["echo -e '42\\n-7\\n+10\\n3.14' | grep -E '^-?[0-9]+$'"]

[[commands]]
id = "shared-455ebd690477634e"
command = "echo -e '# 这是注释\\nname=value\\n# 另一个注释\\nport=8080' | grep '^#'"
summary = "过滤出以 # 号开头的注释行"
simulated_output = "# 这是注释\n# 另一个注释"

[[commands.tokens]]
text = "echo -e '# 这是注释\\nname=value\\n# 另一个注释\\nport=8080' | grep '^#'"
desc = "过滤出以 # 号开头的注释行"

[commands.dictation]
prompt = "过滤出以 # 号开头的注释行"
answers = ["echo -e '# 这是注释\\nname=value\\n# 另一个注释\\nport=8080' | grep '^#'"]

[[commands]]
id = "shared-0ff5b496d91c8b70"
command = "grep -c '^$' /etc/config.conf"
summary = "统计配置文件中空行的数量（^$ 表示行首紧接行尾，即空行）"
simulated_output = "5"

[[commands.tokens]]
text = "grep -c '^$' /etc/config.conf"
desc = "统计配置文件中空行的数量（^$ 表示行首紧接行尾，即空行）"

[commands.dictation]
prompt = "统计配置文件中空行的数量（^$ 表示行首紧接行尾，即空行）"
answers = ["grep -c '^$' /etc/config.conf"]

[[commands]]
id = "shared-396b93f323ae6053"
command = "echo -e 'readme.conf\\nsetup.conf\\nreadme.txt\\nnotes.conf.bak' | grep '\\.conf$'"
summary = "查找以 .conf 结尾的行；用 \\. 转义确保匹配字面量点号，$ 确保在行尾"
simulated_output = "readme.conf\nsetup.conf"

[[commands.tokens]]
text = "echo -e 'readme.conf\\nsetup.conf\\nreadme.txt\\nnotes.conf.bak' | grep '\\.conf$'"
desc = "查找以 .conf 结尾的行；用 \\. 转义确保匹配字面量点号，$ 确保在行尾"

[commands.dictation]
prompt = "查找以 .conf 结尾的行；用 \\. 转义确保匹配字面量点号，$ 确保在行尾"
answers = ["echo -e 'readme.conf\\nsetup.conf\\nreadme.txt\\nnotes.conf.bak' | grep '\\.conf$'"]

[[commands]]
id = "shared-e9268f8002ee7025"
command = "grep -E '^.{0,40}$' data.txt"
summary = "查找长度不超过 40 个字符的行（从行首到行尾最多 40 个任意字符）"
simulated_output = "short line\nthis is a medium line here\nanother short one"

[[commands.tokens]]
text = "grep -E '^.{0,40}$' data.txt"
desc = "查找长度不超过 40 个字符的行（从行首到行尾最多 40 个任意字符）"

[commands.dictation]
prompt = "查找长度不超过 40 个字符的行（从行首到行尾最多 40 个任意字符）"
answers = ["grep -E '^.{0,40}$' data.txt"]

[[commands]]
id = "shared-66eb182d9b67d3a2"
command = "echo -e 'abc\\na1b2\\n123\\n!@#' | grep '[0-9]'"
summary = "匹配包含至少一个数字的行"
simulated_output = "a1b2\n123"

[[commands.tokens]]
text = "echo -e 'abc\\na1b2\\n123\\n!@#' | grep '[0-9]'"
desc = "匹配包含至少一个数字的行"

[commands.dictation]
prompt = "匹配包含至少一个数字的行"
answers = ["echo -e 'abc\\na1b2\\n123\\n!@#' | grep '[0-9]'"]

[[commands]]
id = "shared-c14ba0084337728e"
command = "echo -e 'Hello\\nhello\\nHELLO\\n123abc' | grep '[^a-z]'"
summary = "匹配包含至少一个非小写字母字符的行；hello 纯小写所以不匹配"
simulated_output = "Hello\nHELLO\n123abc"

[[commands.tokens]]
text = "echo -e 'Hello\\nhello\\nHELLO\\n123abc' | grep '[^a-z]'"
desc = "匹配包含至少一个非小写字母字符的行；hello 纯小写所以不匹配"

[commands.dictation]
prompt = "匹配包含至少一个非小写字母字符的行；hello 纯小写所以不匹配"
answers = ["echo -e 'Hello\\nhello\\nHELLO\\n123abc' | grep '[^a-z]'"]

[[commands]]
id = "shared-233669ba9c5a0777"
command = "grep -E '(error|warning)' /var/log/syslog | head -5"
summary = "在系统日志中搜索包含 error 或 warning 的行，使用分组配合交替符"
simulated_output = "Mar  8 08:01:12 server kernel: warning: low memory\nMar  8 08:15:33 server app[921]: error: connection refused\nMar  8 09:22:01 server sshd[1042]: warning: authentication failure\nMar  8 10:05:44 server nginx[780]: error: upstream timeout\nMar  8 10:30:17 server kernel: warning: disk space low"

[[commands.tokens]]
text = "grep -E '(error|warning)' /var/log/syslog | head -5"
desc = "在系统日志中搜索包含 error 或 warning 的行，使用分组配合交替符"

[commands.dictation]
prompt = "在系统日志中搜索包含 error 或 warning 的行，使用分组配合交替符"
answers = ["grep -E '(error|warning)' /var/log/syslog | head -5"]

[[commands]]
id = "shared-980f161ee091bd1d"
command = "echo -e 'haha\\nhahaha\\nha\\nho' | grep -E '^(ha){2,}$'"
summary = "匹配 ha 重复两次或更多次且占满整行的字符串"
simulated_output = "haha\nhahaha"

[[commands.tokens]]
text = "echo -e 'haha\\nhahaha\\nha\\nho' | grep -E '^(ha){2,}$'"
desc = "匹配 ha 重复两次或更多次且占满整行的字符串"

[commands.dictation]
prompt = "匹配 ha 重复两次或更多次且占满整行的字符串"
answers = ["echo -e 'haha\\nhahaha\\nha\\nho' | grep -E '^(ha){2,}$'"]

[[commands]]
id = "shared-90410eaf05b48447"
command = "grep -E '(error|warning)' data.txt"
summary = "在文件 data.txt 中搜索包含 error 或 warning 的行（使用扩展正则表达式）"

[[commands.tokens]]
text = "grep -E '(error|warning)' data.txt"
desc = "在文件 data.txt 中搜索包含 error 或 warning 的行（使用扩展正则表达式）"

[commands.dictation]
prompt = "在文件 data.txt 中搜索包含 error 或 warning 的行（使用扩展正则表达式）"
answers = ["grep -E '(error|warning)' data.txt"]

[[commands]]
id = "shared-dc2482b40f77e7b8"
command = "grep -E 'colou?r' file"
summary = "匹配 color 和 colour 两种拼写（使用扩展正则表达式）"

[[commands.tokens]]
text = "grep -E 'colou?r' file"
desc = "匹配 color 和 colour 两种拼写（使用扩展正则表达式）"

[commands.dictation]
prompt = "匹配 color 和 colour 两种拼写（使用扩展正则表达式）"
answers = ["grep -E 'colou?r' file"]

[[commands]]
id = "shared-317b188e78fd4af9"
command = "grep -E '[0-9]+' log.txt"
summary = "在文件 log.txt 中查找包含一个或多个连续数字的行"

[[commands.tokens]]
text = "grep -E '[0-9]+' log.txt"
desc = "在文件 log.txt 中查找包含一个或多个连续数字的行"

[commands.dictation]
prompt = "在文件 log.txt 中查找包含一个或多个连续数字的行"
answers = ["grep -E '[0-9]+' log.txt"]

[[commands]]
id = "shared-1db263535707bd41"
command = "grep '^error' app.log"
summary = "匹配以 error 开头的日志行"
simulated_output = "error: failed to connect\nerror: timeout reached"

[[commands.tokens]]
text = "grep '^error' app.log"
desc = "匹配以 error 开头的日志行"

[commands.dictation]
prompt = "匹配以 error 开头的日志行"
answers = ["grep '^error' app.log"]

[[commands]]
id = "shared-0187aadaf2258732"
command = "grep -E '\\.conf$' files.txt"
summary = "匹配以 .conf 结尾的文件名"
simulated_output = "nginx.conf\nsshd.conf"

[[commands.tokens]]
text = "grep -E '\\.conf$' files.txt"
desc = "匹配以 .conf 结尾的文件名"

[commands.dictation]
prompt = "匹配以 .conf 结尾的文件名"
answers = ["grep -E '\\.conf$' files.txt"]

[[commands]]
id = "shared-c6c8142aeb08db66"
command = "grep -E '^[0-9]+$' nums.txt"
summary = "查找只包含数字的整行"
simulated_output = "42\n9001"

[[commands.tokens]]
text = "grep -E '^[0-9]+$' nums.txt"
desc = "查找只包含数字的整行"

[commands.dictation]
prompt = "查找只包含数字的整行"
answers = ["grep -E '^[0-9]+$' nums.txt"]

[[commands]]
id = "shared-4f17c34a87c2a9b7"
command = "grep -E '^192\\.168\\.' hosts.txt"
summary = "匹配 IPv4 风格前缀 192.168."
simulated_output = "192.168.1.10\n192.168.1.88"

[[commands.tokens]]
text = "grep -E '^192\\.168\\.' hosts.txt"
desc = "匹配 IPv4 风格前缀 192.168."

[commands.dictation]
prompt = "匹配 IPv4 风格前缀 192.168."
answers = ["grep -E '^192\\.168\\.' hosts.txt"]

[[commands]]
id = "shared-215d10c96b787533"
command = "grep '^#' config.ini"
summary = "过滤以 # 开头的注释行"
simulated_output = "# server config\n# timeout settings"

[[commands.tokens]]
text = "grep '^#' config.ini"
desc = "过滤以 # 开头的注释行"

[commands.dictation]
prompt = "过滤以 # 开头的注释行"
answers = ["grep '^#' config.ini"]

[[commands]]
id = "shared-872fb92583ae0f6e"
command = "grep -E '(warning|error)' app.log"
summary = "匹配含有 warning 或 error 的行"
simulated_output = "warning: disk usage high\nerror: cannot open file"

[[commands.tokens]]
text = "grep -E '(warning|error)' app.log"
desc = "匹配含有 warning 或 error 的行"

[commands.dictation]
prompt = "匹配含有 warning 或 error 的行"
answers = ["grep -E '(warning|error)' app.log"]

[[commands]]
id = "shared-65dc47c6dde0dcfc"
command = "grep -E 'colou?r' words.txt"
summary = "用扩展正则匹配 color 和 colour"

[[commands.tokens]]
text = "grep -E 'colou?r' words.txt"
desc = "用扩展正则匹配 color 和 colour"

[commands.dictation]
prompt = "用扩展正则匹配 color 和 colour"
answers = ["grep -E 'colou?r' words.txt"]

[[commands]]
id = "shared-1ed32f1f82f18201"
command = "grep -c '^$' data.txt"
summary = "统计空行数量（行首行尾锚点）"

[[commands.tokens]]
text = "grep -c '^$' data.txt"
desc = "统计空行数量（行首行尾锚点）"

[commands.dictation]
prompt = "统计空行数量（行首行尾锚点）"
answers = ["grep -c '^$' data.txt"]

[[commands]]
id = "shared-1e825a66604c62d9"
command = "grep -E '^[0-9]{4}-[0-9]{2}-[0-9]{2}$' log.txt"
summary = "匹配 2026-03-14 这种日期格式"

[[commands.tokens]]
text = "grep -E '^[0-9]{4}-[0-9]{2}-[0-9]{2}$' log.txt"
desc = "匹配 2026-03-14 这种日期格式"

[commands.dictation]
prompt = "匹配 2026-03-14 这种日期格式"
answers = ["grep -E '^[0-9]{4}-[0-9]{2}-[0-9]{2}$' log.txt"]

[[commands]]
id = "shared-4a9908cb5ec0b338"
command = "[ -n \"$USER\" ] && printf 'named user\\n'"
summary = "-n 判断字符串非空；] 前的空格不可省略"
simulated_output = "named user"

[[commands.tokens]]
text = "[ -n \"$USER\" ] && printf 'named user\\n'"
desc = "-n 判断字符串非空；] 前的空格不可省略"

[commands.dictation]
prompt = "-n 判断字符串非空；] 前的空格不可省略"
answers = ["[ -n \"$USER\" ] && printf 'named user\\n'"]

[[commands]]
id = "shared-d8ab8bc6c1c7fadf"
command = "(port=443; [[ $port =~ ^[0-9]+$ ]] && printf 'numeric port\\n')"
summary = "在子 shell 中先设置数字字符串 443，再用正则确认只包含数字"
simulated_output = "numeric port"

[[commands.tokens]]
text = "(port=443; [[ $port =~ ^[0-9]+$ ]] && printf 'numeric port\\n')"
desc = "在子 shell 中先设置数字字符串 443，再用正则确认只包含数字"

[commands.dictation]
prompt = "在子 shell 中先设置数字字符串 443，再用正则确认只包含数字"
answers = ["(port=443; [[ $port =~ ^[0-9]+$ ]] && printf 'numeric port\\n')"]

[[commands]]
id = "shared-3c87757a0bbaf51f"
command = "test -d /srv/app && printf 'directory ready\\n'"
summary = "只在 /srv/app 是目录时输出静态提示"
simulated_output = "directory ready"

[[commands.tokens]]
text = "test -d /srv/app && printf 'directory ready\\n'"
desc = "只在 /srv/app 是目录时输出静态提示"

[commands.dictation]
prompt = "只在 /srv/app 是目录时输出静态提示"
answers = ["test -d /srv/app && printf 'directory ready\\n'"]

[[commands]]
id = "shared-231b72421ad17a98"
command = "[ -r settings.ini ] && printf 'readable\\n'"
summary = "从当前用户身份判断 settings.ini 是否可读"
simulated_output = "readable"

[[commands.tokens]]
text = "[ -r settings.ini ] && printf 'readable\\n'"
desc = "从当前用户身份判断 settings.ini 是否可读"

[commands.dictation]
prompt = "从当前用户身份判断 settings.ini 是否可读"
answers = ["[ -r settings.ini ] && printf 'readable\\n'"]

[[commands]]
id = "shared-32b2048ca8c22d48"
command = "(environment=production; [ \"$environment\" = \"production\" ] && printf 'production\\n')"
summary = "在子 shell 中先设置 production，再做精确字符串比较"
simulated_output = "production"

[[commands.tokens]]
text = "(environment=production; [ \"$environment\" = \"production\" ] && printf 'production\\n')"
desc = "在子 shell 中先设置 production，再做精确字符串比较"

[commands.dictation]
prompt = "在子 shell 中先设置 production，再做精确字符串比较"
answers = ["(environment=production; [ \"$environment\" = \"production\" ] && printf 'production\\n')"]

[[commands]]
id = "shared-3eedb84f5258d157"
command = "(port=443; [[ $port -gt 0 && $port -le 65535 ]] && printf 'valid range\\n')"
summary = "在子 shell 中先设置端口 443，再组合检查两个整数边界"
simulated_output = "valid range"

[[commands.tokens]]
text = "(port=443; [[ $port -gt 0 && $port -le 65535 ]] && printf 'valid range\\n')"
desc = "在子 shell 中先设置端口 443，再组合检查两个整数边界"

[commands.dictation]
prompt = "在子 shell 中先设置端口 443，再组合检查两个整数边界"
answers = ["(port=443; [[ $port -gt 0 && $port -le 65535 ]] && printf 'valid range\\n')"]

[[commands]]
id = "shared-103a4a1c32edeed0"
command = "test -r report.txt && printf 'readable\\n' || printf 'not readable\\n'"
summary = "演示组合链；需记住中间 printf 的失败也会触发最后分支"
simulated_output = "readable"

[[commands.tokens]]
text = "test -r report.txt && printf 'readable\\n' || printf 'not readable\\n'"
desc = "演示组合链；需记住中间 printf 的失败也会触发最后分支"

[commands.dictation]
prompt = "演示组合链；需记住中间 printf 的失败也会触发最后分支"
answers = ["test -r report.txt && printf 'readable\\n' || printf 'not readable\\n'"]

[[commands]]
id = "shared-25d70b366e84e2b1"
command = "[ -d /srv/app ] && echo directory"
summary = "判断 /srv/app 是目录，成立时输出 directory"
simulated_output = "directory"

[[commands.tokens]]
text = "[ -d /srv/app ] && echo directory"
desc = "判断 /srv/app 是目录，成立时输出 directory"

[commands.dictation]
prompt = "判断 /srv/app 是目录，成立时输出 directory"
answers = ["[ -d /srv/app ] && echo directory"]

[[commands]]
id = "shared-92401117d2a108f2"
command = "(name=server.log; [[ $name == *.log ]] && echo log)"
summary = "在同一子 shell 中先令 name=server.log，再用 [[ ]] 判断它以 .log 结尾，成立时输出 log"
simulated_output = "log"

[[commands.tokens]]
text = "(name=server.log; [[ $name == *.log ]] && echo log)"
desc = "在同一子 shell 中先令 name=server.log，再用 [[ ]] 判断它以 .log 结尾，成立时输出 log"

[commands.dictation]
prompt = "在同一子 shell 中先令 name=server.log，再用 [[ ]] 判断它以 .log 结尾，成立时输出 log"
answers = ["(name=server.log; [[ $name == *.log ]] && echo log)"]

[[commands]]
id = "shared-142dbaff68cdffed"
command = "[ -z \"$value\" ] && echo empty"
summary = "判断变量 value 为空，成立时输出 empty"
simulated_output = "empty"

[[commands.tokens]]
text = "[ -z \"$value\" ] && echo empty"
desc = "判断变量 value 为空，成立时输出 empty"

[commands.dictation]
prompt = "判断变量 value 为空，成立时输出 empty"
answers = ["[ -z \"$value\" ] && echo empty"]

[[commands]]
id = "shared-ff939914f7063d1d"
command = "[ -n \"$USER\" ] && echo set"
summary = "判断变量 USER 非空，成立时输出 set"
simulated_output = "set"

[[commands.tokens]]
text = "[ -n \"$USER\" ] && echo set"
desc = "判断变量 USER 非空，成立时输出 set"

[commands.dictation]
prompt = "判断变量 USER 非空，成立时输出 set"
answers = ["[ -n \"$USER\" ] && echo set"]

[[commands]]
id = "shared-7d065e899c32c416"
command = "(port=443; [[ $port -gt 0 && $port -le 65535 ]])"
summary = "在同一子 shell 中先令 port=443，再用 [[ ]] 判断它大于 0 且小于等于 65535"
simulated_output = ""

[[commands.tokens]]
text = "(port=443; [[ $port -gt 0 && $port -le 65535 ]])"
desc = "在同一子 shell 中先令 port=443，再用 [[ ]] 判断它大于 0 且小于等于 65535"

[commands.dictation]
prompt = "在同一子 shell 中先令 port=443，再用 [[ ]] 判断它大于 0 且小于等于 65535"
answers = ["(port=443; [[ $port -gt 0 && $port -le 65535 ]])"]

[[commands]]
id = "shared-601cdba065c1480d"
command = "test -f report.txt && test -r report.txt"
summary = "先判断 report.txt 是普通文件，再判断它可读"
simulated_output = ""

[[commands.tokens]]
text = "test -f report.txt && test -r report.txt"
desc = "先判断 report.txt 是普通文件，再判断它可读"

[commands.dictation]
prompt = "先判断 report.txt 是普通文件，再判断它可读"
answers = ["test -f report.txt && test -r report.txt"]

[[commands]]
id = "shared-c62d741ccbdac69b"
command = "test -f settings.ini || echo missing"
summary = "settings.ini 不存在时输出 missing"
simulated_output = "missing"

[[commands.tokens]]
text = "test -f settings.ini || echo missing"
desc = "settings.ini 不存在时输出 missing"

[commands.dictation]
prompt = "settings.ini 不存在时输出 missing"
answers = ["test -f settings.ini || echo missing"]

[[commands]]
id = "shared-ebae5dd0620f5423"
command = "test -f config.yml"
summary = "用 test 判断 config.yml 是普通文件"
simulated_output = ""

[[commands.tokens]]
text = "test -f config.yml"
desc = "用 test 判断 config.yml 是普通文件"

[commands.dictation]
prompt = "用 test 判断 config.yml 是普通文件"
answers = ["test -f config.yml"]

[[commands]]
id = "shared-c789aa5e96596013"
command = "[ -d logs ]"
summary = "用单中括号判断 logs 是目录"
simulated_output = ""

[[commands.tokens]]
text = "[ -d logs ]"
desc = "用单中括号判断 logs 是目录"

[commands.dictation]
prompt = "用单中括号判断 logs 是目录"
answers = ["[ -d logs ]"]

[[commands]]
id = "shared-0c9e33ea7594fc63"
command = "(mode=safe; [ \"$mode\" = safe ])"
summary = "在同一子 shell 中先令 mode=safe，再用单中括号判断它等于 safe"
simulated_output = ""

[[commands.tokens]]
text = "(mode=safe; [ \"$mode\" = safe ])"
desc = "在同一子 shell 中先令 mode=safe，再用单中括号判断它等于 safe"

[commands.dictation]
prompt = "在同一子 shell 中先令 mode=safe，再用单中括号判断它等于 safe"
answers = ["(mode=safe; [ \"$mode\" = safe ])"]

[[commands]]
id = "shared-98f921f277e23426"
command = "[[ $state != failed ]]"
summary = "用 [[ ]] 判断 state 不等于 failed"
simulated_output = ""

[[commands.tokens]]
text = "[[ $state != failed ]]"
desc = "用 [[ ]] 判断 state 不等于 failed"

[commands.dictation]
prompt = "用 [[ ]] 判断 state 不等于 failed"
answers = ["[[ $state != failed ]]"]

[[commands]]
id = "shared-31913d39653f9d3f"
command = "(retries=3; [ \"$retries\" -ge 3 ])"
summary = "在同一子 shell 中先令 retries=3，再判断它大于等于 3"
simulated_output = ""

[[commands.tokens]]
text = "(retries=3; [ \"$retries\" -ge 3 ])"
desc = "在同一子 shell 中先令 retries=3，再判断它大于等于 3"

[commands.dictation]
prompt = "在同一子 shell 中先令 retries=3，再判断它大于等于 3"
answers = ["(retries=3; [ \"$retries\" -ge 3 ])"]

[[commands]]
id = "shared-3a70c2c2f35e58ca"
command = "test -r data.csv && echo ready"
summary = "检查 data.csv 可读，成功后输出 ready"
simulated_output = "ready"

[[commands.tokens]]
text = "test -r data.csv && echo ready"
desc = "检查 data.csv 可读，成功后输出 ready"

[commands.dictation]
prompt = "检查 data.csv 可读，成功后输出 ready"
answers = ["test -r data.csv && echo ready"]

[[commands]]
id = "shared-45846ed0d8c2f786"
command = "test -r data.csv || echo unavailable"
summary = "检查 data.csv 可读，失败时输出 unavailable"
simulated_output = "unavailable"

[[commands.tokens]]
text = "test -r data.csv || echo unavailable"
desc = "检查 data.csv 可读，失败时输出 unavailable"

[commands.dictation]
prompt = "检查 data.csv 可读，失败时输出 unavailable"
answers = ["test -r data.csv || echo unavailable"]

[[commands]]
id = "shared-6684e34b227be834"
command = "name=outer; ( name=inner; printf '%s\\n' \"$name\" ); printf '%s\\n' \"$name\""
summary = "子 Shell 中的变量修改不会覆盖外层变量"
simulated_output = "inner\nouter"

[[commands.tokens]]
text = "name=outer; ( name=inner; printf '%s\\n' \"$name\" ); printf '%s\\n' \"$name\""
desc = "子 Shell 中的变量修改不会覆盖外层变量"

[commands.dictation]
prompt = "子 Shell 中的变量修改不会覆盖外层变量"
answers = ["name=outer; ( name=inner; printf '%s\\n' \"$name\" ); printf '%s\\n' \"$name\""]

[[commands]]
id = "shared-f91b6466bb329358"
command = "name=outer; { name=inner; }; printf '%s\\n' \"$name\""
summary = "花括号不隔离变量，组内赋值会保留到后续命令"
simulated_output = "inner"

[[commands.tokens]]
text = "name=outer; { name=inner; }; printf '%s\\n' \"$name\""
desc = "花括号不隔离变量，组内赋值会保留到后续命令"

[commands.dictation]
prompt = "花括号不隔离变量，组内赋值会保留到后续命令"
answers = ["name=outer; { name=inner; }; printf '%s\\n' \"$name\""]

[[commands]]
id = "shared-385e07c02c27ad81"
command = "( set -- one two three four five six seven eight nine archive.tar; printf '%s\\n' \"${10}\" )"
summary = "先在子 Shell 中建立十个位置参数，再用 ${10} 读取第十个；子 Shell 避免改动外层参数"
simulated_output = "archive.tar"

[[commands.tokens]]
text = "( set -- one two three four five six seven eight nine archive.tar; printf '%s\\n' \"${10}\" )"
desc = "先在子 Shell 中建立十个位置参数，再用 ${10} 读取第十个；子 Shell 避免改动外层参数"

[commands.dictation]
prompt = "先在子 Shell 中建立十个位置参数，再用 ${10} 读取第十个；子 Shell 避免改动外层参数"
answers = ["( set -- one two three four five six seven eight nine archive.tar; printf '%s\\n' \"${10}\" )"]

[[commands]]
id = "shared-c16b086e7b41dd34"
command = "file=photo.jpeg; printf '%s\\n' \"${file%.jpeg}.jpg\""
summary = "删除最短 .jpeg 后缀，再拼接 .jpg"
simulated_output = "photo.jpg"

[[commands.tokens]]
text = "file=photo.jpeg; printf '%s\\n' \"${file%.jpeg}.jpg\""
desc = "删除最短 .jpeg 后缀，再拼接 .jpg"

[commands.dictation]
prompt = "删除最短 .jpeg 后缀，再拼接 .jpg"
answers = ["file=photo.jpeg; printf '%s\\n' \"${file%.jpeg}.jpg\""]

[[commands]]
id = "shared-f0f01861e587069f"
command = "printf '%s\\n' item-{1..3}"
summary = "生成 item-1、item-2、item-3，不需要目录中存在同名文件"
simulated_output = "item-1\nitem-2\nitem-3"

[[commands.tokens]]
text = "printf '%s\\n' item-{1..3}"
desc = "生成 item-1、item-2、item-3，不需要目录中存在同名文件"

[commands.dictation]
prompt = "生成 item-1、item-2、item-3，不需要目录中存在同名文件"
answers = ["printf '%s\\n' item-{1..3}"]

[[commands]]
id = "shared-65e977b61f211a90"
command = "items='alpha beta'; printf '<%s>\\n' $items"
summary = "未引用展开先发生单词分割，因此 printf 接收到两个参数"
simulated_output = "<alpha>\n<beta>"

[[commands.tokens]]
text = "items='alpha beta'; printf '<%s>\\n' $items"
desc = "未引用展开先发生单词分割，因此 printf 接收到两个参数"

[commands.dictation]
prompt = "未引用展开先发生单词分割，因此 printf 接收到两个参数"
answers = ["items='alpha beta'; printf '<%s>\\n' $items"]

[[commands]]
id = "shared-54fe047a831d3577"
command = "printf '%s\\n' '$HOME'"
summary = "单引号阻止参数展开，输出字面量 $HOME"
simulated_output = "$HOME"

[[commands.tokens]]
text = "printf '%s\\n' '$HOME'"
desc = "单引号阻止参数展开，输出字面量 $HOME"

[commands.dictation]
prompt = "单引号阻止参数展开，输出字面量 $HOME"
answers = ["printf '%s\\n' '$HOME'"]

[[commands]]
id = "shared-dfcc30556f0344b6"
command = "{ echo alpha; echo beta; }"
summary = "用花括号分组依次输出 alpha 和 beta"
simulated_output = "alpha\nbeta"

[[commands.tokens]]
text = "{ echo alpha; echo beta; }"
desc = "用花括号分组依次输出 alpha 和 beta"

[commands.dictation]
prompt = "用花括号分组依次输出 alpha 和 beta"
answers = ["{ echo alpha; echo beta; }"]

[[commands]]
id = "shared-2c9dd5e154b72b51"
command = "base=report; echo \"${base}_old\""
summary = "先把 base 设为 report，再拼接字面后缀 _old，并避免把 base_old 当变量名"
simulated_output = "report_old"

[[commands.tokens]]
text = "base=report; echo \"${base}_old\""
desc = "先把 base 设为 report，再拼接字面后缀 _old，并避免把 base_old 当变量名"

[commands.dictation]
prompt = "先把 base 设为 report，再拼接字面后缀 _old，并避免把 base_old 当变量名"
answers = ["base=report; echo \"${base}_old\""]

[[commands]]
id = "shared-2621989b64da1354"
command = "echo \"${APP_MODE:-development}\""
summary = "APP_MODE 为空或未设置时输出 development，但不修改变量"
simulated_output = "development"

[[commands.tokens]]
text = "echo \"${APP_MODE:-development}\""
desc = "APP_MODE 为空或未设置时输出 development，但不修改变量"

[commands.dictation]
prompt = "APP_MODE 为空或未设置时输出 development，但不修改变量"
answers = ["echo \"${APP_MODE:-development}\""]

[[commands]]
id = "shared-9831cf4cb4036f78"
command = "( echo one; echo two )"
summary = "在隔离的子 Shell 中输出 one 和 two"
simulated_output = "one\ntwo"

[[commands.tokens]]
text = "( echo one; echo two )"
desc = "在隔离的子 Shell 中输出 one 和 two"

[commands.dictation]
prompt = "在隔离的子 Shell 中输出 one 和 two"
answers = ["( echo one; echo two )"]

[[commands]]
id = "shared-50416fe26e095911"
command = "{ echo one; echo two; }"
summary = "在当前 Shell 分组中依次输出 one 和 two"
simulated_output = "one\ntwo"

[[commands.tokens]]
text = "{ echo one; echo two; }"
desc = "在当前 Shell 分组中依次输出 one 和 two"

[commands.dictation]
prompt = "在当前 Shell 分组中依次输出 one 和 two"
answers = ["{ echo one; echo two; }"]

[[commands]]
id = "shared-6bd5cbd7be3fbc71"
command = "file=photo.jpeg; echo \"${file%.jpeg}.jpg\""
summary = "先把 file 设为 photo.jpeg，再删除末尾最短的 .jpeg 并拼接 .jpg"
simulated_output = "photo.jpg"

[[commands.tokens]]
text = "file=photo.jpeg; echo \"${file%.jpeg}.jpg\""
desc = "先把 file 设为 photo.jpeg，再删除末尾最短的 .jpeg 并拼接 .jpg"

[commands.dictation]
prompt = "先把 file 设为 photo.jpeg，再删除末尾最短的 .jpeg 并拼接 .jpg"
answers = ["file=photo.jpeg; echo \"${file%.jpeg}.jpg\""]

[[commands]]
id = "shared-3a3997ce8dd61130"
command = "echo item-{1..3}"
summary = "用花括号序列生成 item-1 到 item-3"
simulated_output = "item-1 item-2 item-3"

[[commands.tokens]]
text = "echo item-{1..3}"
desc = "用花括号序列生成 item-1 到 item-3"

[commands.dictation]
prompt = "用花括号序列生成 item-1 到 item-3"
answers = ["echo item-{1..3}"]

[[commands]]
id = "shared-0b226b5b9d346236"
command = "pkill -f \"python script.py\""
summary = "终止命令行匹配 \"python script.py\" 的所有进程"
simulated_output = ""

[[commands.tokens]]
text = "pkill -f \"python script.py\""
desc = "终止命令行匹配 \"python script.py\" 的所有进程"

[commands.dictation]
prompt = "终止命令行匹配 \"python script.py\" 的所有进程"
answers = ["pkill -f \"python script.py\""]

[[commands]]
id = "shared-8a2713171f0d18b8"
command = "sudo systemctl restart nginx"
summary = "重启 nginx 服务"
simulated_output = ""

[[commands.tokens]]
text = "sudo systemctl restart nginx"
desc = "重启 nginx 服务"

[commands.dictation]
prompt = "重启 nginx 服务"
answers = ["sudo systemctl restart nginx"]

[[commands]]
id = "shared-7fac98fbafa8e3fe"
command = "sudo systemctl enable nginx"
summary = "将 nginx 设为开机自启"
simulated_output = "Synchronizing state of nginx.service with SysV service script with /lib/systemd/systemd-sysv-install.\nExecuting: /lib/systemd/systemd-sysv-install enable nginx\nCreated symlink /etc/systemd/system/multi-user.target.wants/nginx.service → /lib/systemd/system/nginx.service."

[[commands.tokens]]
text = "sudo systemctl enable nginx"
desc = "将 nginx 设为开机自启"

[commands.dictation]
prompt = "将 nginx 设为开机自启"
answers = ["sudo systemctl enable nginx"]

[[commands]]
id = "shared-4a16214771f9510c"
command = "sudo systemctl disable nginx"
summary = "取消 nginx 的开机自启"
simulated_output = "Synchronizing state of nginx.service with SysV service script with /lib/systemd/systemd-sysv-install.\nExecuting: /lib/systemd/systemd-sysv-install disable nginx\nRemoved /etc/systemd/system/multi-user.target.wants/nginx.service."

[[commands.tokens]]
text = "sudo systemctl disable nginx"
desc = "取消 nginx 的开机自启"

[commands.dictation]
prompt = "取消 nginx 的开机自启"
answers = ["sudo systemctl disable nginx"]

[[commands]]
id = "shared-3d704df1bd62dbcb"
command = "systemctl is-enabled nginx"
summary = "检查 nginx 是否已设为开机自启"
simulated_output = "enabled"

[[commands.tokens]]
text = "systemctl is-enabled nginx"
desc = "检查 nginx 是否已设为开机自启"

[commands.dictation]
prompt = "检查 nginx 是否已设为开机自启"
answers = ["systemctl is-enabled nginx"]
