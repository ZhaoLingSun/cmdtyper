services:
  cmdtyper:
    build: .
    image: cmdtyper:v0.3
    container_name: cmdtyper
    stdin_open: true
    tty: true
    volumes:
      - cmdtyper_data:/userdata
    environment:
      - TERM=xterm-256color
      - LANG=zh_CN.UTF-8

volumes:
  cmdtyper_data:
